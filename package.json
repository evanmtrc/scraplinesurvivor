{
  "name": "scrapline-survivor",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "test": "node tests/systems.test.mjs",
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.0"
  }
}
